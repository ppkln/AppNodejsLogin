<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Profile List</title>
</head>
<body>
    <div class="container">
        <h7>ผู้ที่กำลังใช้งานระบบ : คุณ <%= UserLoggedin%></h7>
        <div class="card">
            <div class="card-body">
                <div class="text-center"><h1>รายการสมาชิกทั้งหมดของระบบ</h1></div>
                <table class="table table-bordered text-center">
                    <thead>
                        <tr>
                            <th>id</th>
                            <th>name</th>
                            <th>email</th>
                            <th>edit</th>
                            <th>delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% Object.values(profiles).forEach(function(item){%>
                        <tr>
                            <td><%= item.id%></td>
                            <td><%= item.name%></td>
                            <td><%= item.email%></td>
                            <td>
                                <form action="/editfrm" method="POST">
                                    <input type="hidden" name="edit_id" value="<%= item.id%>">
                                    <button type="submit" class="btn btn-primary">แก้ไข</button>
                                </form>
                            </td>
                            <td><a class="btn btn-danger" href="/delete/<%= item.id%>" onclick="return confirm('คุณต้องการลบข้อมูลหรือไม่ ?')">ลบ</a></td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
                <a href="/" class="btn btn-primary mt-2 me-2">Home</a>
                <a href="/logout" class="btn btn-danger mt-2 me-2">Logout</a>
            </div>
        </div>
    </div>
</body>
</html>